# Historical acceptance snapshot — superseded by MVP scope

Prepared September 12, 2026, during the read-only completion audit. The user then replaced the original 19-gate completion plan with a two-hour MVP deadline ending at 14:28 UTC. This document is a historical checkpoint, **not a current completion gate or release decision**. Use the new MVP goal and API contract maintained by the primary agent for current scope.

At checkpoint `d9ec87c`, [build-progress.md](../build-progress.md) records a successful integrated native build, 26 passing local Core/transport tests, the separately exercised real URLSession/Vapor boundary, 8 local server tests, and 26 device-adapter checks. Live PostgreSQL and physical camera/microphone capture were not verified. Earlier failure notes in [client-verification.md](client-verification.md) are superseded by the later successful execution recorded in progress.

The primary had verified imported text → review/save → relevant brief → original implant PDF page 2, source-title edits preserving page provenance and marking the brief stale, edited questions surviving regeneration, booking confirmation without duplicates, sample memory persistence after relaunch, visit creation/title edits, stable date-only editing, and the native PDF/share presentation. New transcript corrections and saved-memory source links had passed a full-app typecheck and 17 persistence checks; their UI walkthrough was still pending. Appearance, smaller/larger text, reset, final review, as-built architecture, and remote push were not yet fully evidenced at this snapshot. None is declared complete here.

The original T26 wording does not require normalized tables for every domain. The approved [implementation contract](../implementation-contract.md) and [server README](../../server/README.md) deliberately use an owner-scoped JSONB aggregate, BYTEA attachments, and mutation audit metadata. All prototype domains are represented inside that snapshot. SQL foreign keys apply to owner/attachment/audit relationships; inner-domain relationships are primarily client-validated. Mutation UUIDs identify audit events; state revisions, stable attachment IDs, and persisted booking identity provide their respective duplicate protections. No general client replay-key cache or transaction spanning every attachment request plus snapshot write is claimed.

The original T21 documentation described future booking tools, authentication, and reconciliation conceptually; concrete provider wire shapes were still being finalized when scope changed. The new MVP work is adding configurable Gemini, speech, and ElevenLabs adapters. This historical review does not establish their implementation or verification status, and no live credentials or actual calls were exercised by this reviewer.

Only this review file was written for the curtailed audit. No app, server, test, checklist, or build changes were performed.
