# Native audit repairs — September 12, 2026

The eight assigned native defects have focused repairs and deterministic regression checks. Changes were made on the isolated `fix/audit-native-20260912` worktree. The main checkout's unrelated account and tab-interaction work was inspected only for overlap and was not included.

| Finding | Repair | Executed regression evidence |
| --- | --- | --- |
| RVA-10-001 | Provider publication captures both connection and workspace generations. Direct snapshot replacements invalidate pending operations; ordinary saved field edits retain the workspace generation. Summary, preparation, transcription, call submission and call status guard success and error publication. | 60 cases cover five operations, token/URL changes, switch away and back, direct replacement, actual pull and demo reset, with stale success and failure responses. Normal publication still preserves unrelated profile and notes edits. The stable submitted call request remains available after a connection switch. |
| RVA-03-001 | Startup repair errors report a nonblocking error while preserving the loaded snapshot. Restoring demo data from the recovery screen requires explicit confirmation. | Filesystem failures in both the legacy label repair and interrupted-booking repair retain valid state with no startup error. A later launch completes repair when the obstruction is removed. Confirmation UI was source-reviewed. |
| RVA-04-001 | Visits sort by parsed instants and use IDs to break ties. | Mixed UTC/offset dates put the earlier instant first, including an equivalent-instant tie. |
| RVA-04-005 | The record editor retains its opening baseline in SwiftUI state and merges only edited fields into the latest record. Actual text edits regenerate the excerpt and clear stale page segmentation. | A notes save after an actual provider summary preserves summary, provenance, version and pages. Actual source edits refresh the excerpt. Stale source review and deleted records are rejected. |
| RVA-05-001 | Recording notes save by recording ID and update only separate notes. | Notes saved after actual transcription preserve newer segments, model, status and audio reference. A deleted recording is not recreated. |
| RVA-05-002 | Failed finalization exposes an explicit terminal failure state and a discard/restart control. Resume no longer incorrectly reports a one-hour limit for that failure. | Production recorder methods reject both short audio and decoder failure, preserve the draft until discard, publish accurate recovery guidance, reset on discard and permit a subsequent successful recording. |
| RVA-05-008 | A finalized recording draft retains the same metadata and audio filename until persistence succeeds. The session offers retry and guards dismissal while unsaved. | A real repository save failure leaves the production save draft and original bytes intact. Retrying saves the same recording identity and file, clears the draft only after success and creates no duplicate entry. Retry/dismissal UI was source-reviewed. |
| RVA-04-004 | The initial connected brief action displays provider busy context and disables while busy. Its store entry point also explains when retry is needed. | A busy attempt makes no provider request and publishes the retry notice; after busy clears, preparation succeeds. |

Run `python3 scripts/check_native_repairs.py` from the repository root. The final focused run passed against native repair commit `0902e3f` with the shared excerpt API from `371e69c`. Strict Swift formatting passed for all 16 native production/harness files changed by this repair; the source structure check and `git diff --check` passed.

The state harness compiles unchanged production AppStore/domain/storage sources with controlled provider and server doubles. Audio checks compile the production `AudioServices.swift` method bodies with only the AVFoundation/UIKit imports substituted by deterministic device/codec doubles. Test build products, snapshots and synthetic original bytes live under `/private/tmp` during execution. No network, paid provider, phone call, microphone, simulator UI, or real user data is used.

These checks establish client state, merge, persistence and lifecycle behavior. They do not establish real microphone permissions, interruption handling, codec output, physical-device behavior, rendered SwiftUI controls, live authentication/provider operation, or the complete iOS build; those remain integration or device checks. The initial native commits are `ecac23b`, `5b347df`, `e4202ed`, and `0902e3f`; the last is the native call-site integration with the shared excerpt repair.
