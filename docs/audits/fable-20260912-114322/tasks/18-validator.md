# Track 18: Independent data and functionality validator

Read the workflow, immutable snapshot manifest, completed reviewer reports and shared runner evidence. Reproduce candidate findings concerning data loss, evidence misrepresentation, stale writes, source identity, user journeys, report/export and transcript memory. Challenge false positives with exact current code and focused synthetic checks requested from runner.

Do not alter code or settings, call live services, read credentials or duplicate the runner's builds. Use precise source reasoning and coordinate focused reproduction with the runner. For every candidate P0/P1/P2 finding, record confirmed/rejected/needs evidence/WIP with reason, exact file/line and trigger. Deduplicate cross-track overlaps. No requirement to find issues. Deliver `reports/18-validation.md` and `reports/18-validation.json` for the coordinator. Highest-confidence consequential findings come before style suggestions.
