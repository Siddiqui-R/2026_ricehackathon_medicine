# Track 01 — Spec and feature traceability (wave A, source reviewer)

Snapshot: `fable-audit-20260912-114322 @ cfe0997+wip`. Reviewer: Track 01 subagent (Fable 5.1), read-only. Assigned checklist IDs: A01 A02 A03 B03 C05 L02 N04. Scope: `docs/**`, `README.md`, `design/**` in the frozen worktree `/Users/tempadmin/Documents/Reva/.worktrees/fable-audit-20260912-114322`. Every cited line below was printed with `sed -n` in that worktree before being recorded. Nothing was edited, built, installed, or run inside the worktree beyond `cat`/`sed`/`grep`/`find`/read-only `git diff|log|show|status|worktree list`, one read-only `pypdf` text extraction of the checked-in packet PDF, and a scratchpad Python link checker that only opened files for reading.

## 1. Scope and inventory

Scope file count: 55 files (`find docs README.md design -type f`). 42 text files were read in full; 13 binaries were listed but not opened (see section 9). Line counts are `wc -l`.

| File | Lines | Coverage |
| --- | --- | --- |
| `README.md` | 122 | fully read (WIP-modified: line 54 model rename) |
| `docs/architecture.md` | 226 | fully read (WIP-modified: line 86) |
| `docs/build-progress.md` | 160 | fully read |
| `docs/coding-standard.md` | 105 | fully read |
| `docs/completion-criteria.md` | 40 | fully read |
| `docs/deployment-vercel.md` | 38 | fully read |
| `docs/implementation-contract.md` | 61 | fully read |
| `docs/mvp-goal.md` | 19 | fully read |
| `docs/optimization-review-checklist.md` | 359 | fully read |
| `docs/project-overview-packet.md` | 226 | fully read (WIP-modified: line 168) |
| `docs/reva-design-references.md` | 107 | fully read |
| `docs/reva-stack-spec.md` | 446 | fully read |
| `docs/reva-style-plan.md` | 183 | fully read |
| `docs/reviews/01-architecture.md` | 56 | fully read |
| `docs/reviews/02-implementation.md` | 66 | fully read |
| `docs/reviews/03-targeted-revision.md` | 54 | fully read |
| `docs/reviews/04-heart-red-palette.md` | 63 | fully read |
| `docs/reviews/acceptance-audit.md` | 50 | fully read |
| `docs/reviews/client-verification.md` | 27 | fully read |
| `docs/reviews/final-gate-audit.md` | 13 | fully read |
| `docs/reviews/mvp-api-contract-review.md` | 42 | fully read |
| `docs/reviews/review-log.md` | 29 | fully read |
| `docs/task-list.md` | 338 | fully read |
| `docs/task-specs/backend.md` | 51 | fully read |
| `docs/task-specs/client-server-integration.md` | 39 | fully read |
| `docs/task-specs/client-verification.md` | 32 | fully read |
| `docs/task-specs/demo-dataset.md` | 75 | fully read |
| `docs/task-specs/device-services.md` | 101 | fully read |
| `docs/task-specs/final-review.md` | 13 | fully read |
| `docs/task-specs/mvp-api-contract.md` | 27 | fully read |
| `docs/task-specs/provider-client-tests.md` | 30 | fully read |
| `docs/task-specs/transcript-correction.md` | 61 | fully read |
| `docs/task-specs/web-client.md` | 54 | fully read |
| `docs/team-workflow.md` | 106 | fully read |
| `docs/verification/README.md` | 52 | fully read |
| `docs/verification/code-organization.md` | 32 | fully read |
| `docs/verification/profile-symptoms.md` | 35 | fully read |
| `docs/verification/web-client.md` | 56 | fully read |
| `docs/verification/web-responsive-metrics.json` | 177 | fully read |
| `docs/verification/orthopedic-brief.pdf` | binary | not opened |
| `docs/verification/screenshots/*.png` (12 files) | binary | listed only, not opened |
| `design/README.md` | 20 | fully read |
| `design/palette.json` | 86 | fully read |
| `design/palette-supplied.json` | 108 | fully read |

Files outside the assigned scope that were read only to trace documentation claims (no findings are raised against their code; their owners are other tracks): `apps/web/README.md` (95 lines, fully read; WIP-modified lines 18 and 52), `server/README.md` (106 lines, fully read), `apps/web/src/core/COMPATIBILITY.md` (28 lines), `apps/web/src/main.tsx` (63 lines, WIP), `apps/web/src/landing/Landing.tsx` (69 lines, untracked WIP), `apps/web/src/landing/AccountGate.tsx` (48 lines, untracked WIP), `apps/web/vercel.json` (9 lines, untracked WIP), `vercel.json` (20 lines, committed in cfe0997), `.env.example` (26 lines, placeholders only), `.audit-source-delta.patch` (305 lines), `apps/web/src/styles/landing.css` (header lines 1–6 only), `demo/README.md` (lines 1–40 only), `demo/demo-script.md` (lines 1–30 only), `output/pdf/reva-project-overview-packet.pdf` (text extracted read-only with `pypdf` and grepped for model/baseline strings only). Source trees `apps/ios`, `apps/web/src`, `server`, `scripts`, `Tests` were listed with `find` for the feature matrix; their contents were not reviewed except the read-only greps recorded in section 5 and the concurrent contract.

Evidence-runner logs consulted (audit directory, not the worktree): `evidence/00-snapshot-verification.log`, `evidence/commands.jsonl` (20 entries, stage runner1), `r1-02-structure-check.log`, `r1-05-format-check.log`, `r1-06b-typecheck-after-assets.log`, `r1-07-vitest.log`, `r1-07b-vitest-after-assets.log`, `r1-08-serve-test.log`, `r1-09-web-build.log`, `r1-10-swift-test-root.log`, `r1-11-swift-build-server.log` (tail only), `r1-21-dist-inventory.log`, `r1-03b-swift-format-lint-explicit.log`.

## 2. Snapshot identity (A01)

- Frozen worktree HEAD: `cfe09971443989396b8ef6a14cdf935adba86ddd` (detached), commit `fix: configure Vercel to build and publish the browser app`, dated Sat Sep 12 10:54:38 2026 -0500; it added `apps/web/README.md` (+4), `docs/build-progress.md` (+9), `docs/deployment-vercel.md` (+38, new) and root `vercel.json` (+20, new). Prior commits: `dc25e87`, `6caf388`, `2640360` (browser client), `678e742` (heart-red palette).
- Dirty tracked files (13, `git status --porcelain`): `.env.example`, `README.md`, `apps/web/README.md`, `apps/web/scripts/serve.mjs`, `apps/web/scripts/serve.test.mjs`, `apps/web/src/main.tsx`, `docs/architecture.md`, `docs/project-overview-packet.md`, `scripts/build_overview_packet.py`, `server/.env.example`, `server/README.md`, `server/Sources/RevaServer/Providers/ProviderConfiguration.swift`, `server/Tests/RevaServerTests/GeminiProviderTests.swift`.
- Captured untracked first-party files (4): `apps/web/src/landing/Landing.tsx`, `apps/web/src/landing/AccountGate.tsx`, `apps/web/src/styles/landing.css`, `apps/web/vercel.json`. The worktree also holds the untracked `.audit-source-delta.patch` (audit artifact, 305 lines).
- Hashes: `snapshot-manifest.json` (audit dir; `createdAt` 2026-09-12T11:43:22, `baseCommit` cfe0997…, `patchSHA256` `ae939f8a…`) verified by `evidence/00-snapshot-verification.log`: `manifest files ok=267 mismatch=0 missing=0`, `patch sha256 match: True`.
- Worktrees (`git worktree list`): main checkout `/Users/tempadmin/Documents/Reva` at `475eee7 [main]` (already advanced past cfe0997 — the live checkout is being edited concurrently and was not read except the contract file); `.worktrees/fable-audit-20260912-114322` at `cfe0997` (detached, this snapshot); `.worktrees/final-review` at `d9ec87c [review/final]`; `.worktrees/implementation-review` at `eb2ec3a [review/implementation]`.
- Active implementation changes in the WIP delta (from `.audit-source-delta.patch` and `git diff -U0`): (a) Gemini default model `gemini-2.5-flash` → `gemini-3.8-flash` with `gemini-3.5-flash-lite` as the cheaper alternative in `ProviderConfiguration.swift`, `GeminiProviderTests.swift`, `README.md:54`, `docs/architecture.md:86`, `docs/project-overview-packet.md:168`, `scripts/build_overview_packet.py`, both `.env.example` files, `server/README.md`; (b) browser pathname routing in `apps/web/src/main.tsx` (`/` landing, `/login`+`/signup` static `AccountGate`, `/demo` workspace; legacy `/#/…` forwarded to `/demo#/…`), `serve.mjs:107` `appRoutes = new Set(['/', '/demo', '/login', '/signup'])`, `serve.test.mjs:326–347` (routes served; `/account` and `/demo/records` rejected), nested `apps/web/vercel.json` rewriting `/demo`, `/login`, `/signup` → `/index.html`, and `apps/web/README.md:18,52`.
- Account/session/Tiger-account code is absent from the snapshot: `grep -rIl` for `v1/auth|bcrypt|002_accounts|reva_users|reva_sessions|REVA_ACCOUNTS|REVA_SIGNUP|REVA_SESSION_DAYS|REVA_ALLOWED_ORIGINS|tiger_provision|tiger-setup|AccountStore|VITE_REVA_API_ORIGIN|reva.session.v1` returned no files; `server/Dockerfile`, `server/.dockerignore`, `scripts/tiger_provision.py`, `docs/tiger-setup.md`, `apps/web/src/core/session.ts`, `apps/web/src/core/auth.ts`, `server/Sources/RevaServer/Migrations/002_accounts.sql` do not exist; no `'/app'` route in `serve.mjs`, `main.tsx` or either `vercel.json`. Per the hard rules this is WIP, not a defect.

## 3. Concurrent contract provenance

- Path: `/Users/tempadmin/Documents/Reva/docs/task-specs/accounts-and-tiger.md` (live main checkout; untracked in main; not present in the frozen snapshot). Size 16,322 bytes, 117 lines, mtime Sep 12 11:46 (2026). Title line 1: `Accounts, sessions, and Tiger persistence — implementation contract v1`; line 3: `Written September 12, 2026 for concurrent implementation`. Author session per `reports/00-supervisor-brief.md`: the concurrent Claude session "Reva architecture review". Read read-only as the single permitted live-checkout file.
- Scope it defines (summarised, lines cited): identity `u_`+24 hex owner IDs coexisting with static `REVA_TOKENS` (§1); `BearerMiddleware(tokens:accounts:)` with `rs_`-prefixed 46-character session tokens stored hashed (line 71); web pathname routes `/` landing "(exists)", real `/signup` and `/login` forms "replacing `AccountGate`", and a signed-in `/app` workspace with account-mode store, 1.5 s debounced auto-push and account cards in Settings (lines 88–101); vitest and `serve.test.mjs` coverage for `/app`, and `serve.mjs` + `vercel.json` adding the `/app` route (lines 102–103); Tiger `DATABASE_URL` form, `server/Dockerfile`, `docs/tiger-setup.md`, Vercel env `VITE_REVA_API_ORIGIN` (lines 108–110); ownership A server / B web / C deploy (lines 112–116), "No commits, no `git stash`, no paid or live provider calls, no secrets in any file" (line 117).
- Relationship to the snapshot: the snapshot's landing and static gate are the "exists" baseline the contract builds on; everything else in the contract is emerging scope and is classified WIP in the matrix below.

## 4. Spec contradiction resolutions (A03)

| Topic | Historical statement | Current authority | Resolution |
| --- | --- | --- | --- |
| Palette | `docs/task-list.md:16` exact Ivory/Gold/Slate/Teal/Aqua/Sky; `docs/verification/README.md:10` "Ivory canvas/Sky cards/Teal actions … visually verified"; `docs/verification/profile-symptoms.md:11` Sky/Ivory roles; `design/README.md:18` "The shipped MVP uses Sky canvas, Ivory cards, Teal actions" (inside the section headed "superseded, retained" at line 5); `docs/reva-style-plan.md:76–111` A–D and dark proposals | `docs/reva-style-plan.md:1` (current implementation authority, light only, heart-red); `design/README.md:3`; `design/palette.json:9–46,85`; `README.md:5`; `docs/architecture.md:143`; `docs/build-progress.md:131–132,139`; browser tokens verified `docs/verification/web-client.md:42` | Heart-red/ivory light palette supersedes teal and all dark/adaptive proposals. `docs/verification/README.md:1`, `docs/task-list.md:1`, `docs/completion-criteria.md:1` carry superseded banners; `profile-symptoms.md` does not (recorded as "owner's call" in `docs/reviews/04-heart-red-palette.md:49`). Native heart-red visual evidence is absent (RVA-01-006). |
| Browser client | `docs/mvp-goal.md:15` "web wrapping" deferred; `docs/task-list.md:15` "Browser wrapping is a later phase"; `docs/reva-stack-spec.md:14` "Browser interface is a later phase"; `docs/project-overview-packet.md:7` "No production web app", `:155` "No … browser wrapper … is built" (baseline `d0af1df`, line 3) | `docs/build-progress.md:131` user authorization; `docs/task-specs/web-client.md:3`; `README.md:24–34,103` ("The browser extension is implemented"); `docs/architecture.md:3,27`; `docs/verification/web-client.md`; runner `r1-07b` (77 vitest passed), `r1-08` (7 wrapper tests), `r1-09` (build) | Browser is active, not deferred. `mvp-goal.md` and `reva-stack-spec.md` lack superseded banners (RVA-01-002). |
| Accounts / identity | `docs/reva-stack-spec.md:71` "Google Cloud Identity Platform … Reva does not implement password storage"; `server/README.md:29` "There is no public user registration or real authentication provider in this prototype"; `AccountGate.tsx:9` "Accounts are deferred" | Concurrent contract (section 3) specifying bcrypt on the Swift server, hashed session tokens, `/v1/auth/*` | Accounts, sessions and Tiger-backed per-user state are emerging scope under the concurrent contract; the Identity Platform design is historical. Nothing in the snapshot implements it; classified WIP. |
| Cloud Storage / Cloud Tasks / Document AI / SwiftData / 50 MB / Vertex | `docs/reva-stack-spec.md:62,72–75,95,168` | `docs/architecture.md` (local-authoritative clients, explicit push/pull CAS, JSONB/BYTEA Postgres, 16 MiB/64 MiB/128 attachment limits, Gemini Developer API `generateContent`, local Vision/Tesseract OCR); `server/README.md:45,57,92`; `docs/reviews/mvp-api-contract-review.md:29` (Vertex vs Developer API resolved earlier) | Historical proposal only; `reva-stack-spec.md:4` says it "authorizes no implementation" but has no superseded banner (RVA-01-002). |
| Gemini model | `README.md:54`, `docs/architecture.md:86`, `docs/project-overview-packet.md:168` said `gemini-2.5-flash` at cfe0997 | WIP delta renames to `gemini-3.8-flash` (+ `gemini-3.5-flash-lite`), `server/README.md:92,102`, `.env.example:16–17` | WIP; the checked-in packet PDF still says `gemini-2.5-flash` (RVA-01-003). |
| Hosting | `docs/deployment-vercel.md:11,18,20`; `docs/build-progress.md:157`; `apps/web/README.md:34` (Root Directory = repository root; hash routes need no rewrite) | WIP `apps/web/src/main.tsx:20–35` pathname routes; nested `apps/web/vercel.json`; `apps/web/README.md:52` (Root Directory = `apps/web`) | Unresolved contradiction inside the snapshot (RVA-01-001). |
| MyChart/FHIR, custom password-derived encryption, automatic clinic confirmation, live cellular capture, production certification | `docs/mvp-goal.md:15`; `README.md:103`; `apps/web/README.md:77–78`; `server/README.md:98` | same | Consistently excluded from the MVP; no contradiction. |

## 5. Feature × implementation matrix (A02)

Legend for "Classification": **working** = source present and a fresh runner test/build in this audit exercised the path (unit/integration level; no UI session was run by this track); **partial** = implemented with documented limits or requiring manual configuration that this audit cannot exercise; **WIP** = uncommitted delta or concurrent-contract scope only; **deferred** = explicitly outside the MVP; **unverified** = documented and source present but no fresh runner evidence yet. "Doc claim" is what the repository documentation asserts. File existence comes from `find` listings in the frozen worktree; file contents were not reviewed by this track (they belong to tracks 02–16).

| Feature | Native (apps/ios/Reva/) | Browser (apps/web/src/) | Server (server/Sources/RevaServer/) | Evidence path | Doc claim | Classification |
| --- | --- | --- | --- | --- | --- | --- |
| Records: native import, extraction, originals retained | `Device/DocumentImportService.swift`, `Features/Records/AddRecordView.swift`, `RecordDetailView.swift`, `RecordEditorView.swift`, `RecordsView.swift`, `SourcePreview.swift`, `State/AppStore+Records.swift`, `Core/LocalRepository.swift` | `features/records/ImportDialog.tsx`, `RecordsPage.tsx`, `RecordDetail.tsx`, `RecordEditor.tsx`, `SourcePreview.tsx`, `extractDocument.ts`, `core/repository.ts` | attachments `HTTP.swift`, `LocalFileStore.swift`, `PostgresStore.swift`, `Migrations/001_snapshot.sql` | `README.md:37–45`; `apps/web/README.md:62–63,74–75`; `docs/verification/web-client.md:22–23`; `docs/verification/README.md` (historical); runner `r1-07b` (`repository.test.ts` 7 tests), `r1-10` (44 native tests, 1 skipped) | working locally; originals retained | working (test level); UI unverified in this audit |
| OCR (native VisionKit/Vision; browser Tesseract.js) | `Device/DocumentScanner.swift` | `features/records/extractDocument.ts`; bundled `public/ocr` (14 files, 47.8 MB raw per `r1-21`) | none (local only) | `apps/web/README.md:63,75,78`; `docs/verification/web-client.md:23`; `docs/task-specs/web-client.md:52`; `r1-09`/`r1-21` (OCR assets built into dist) | reviewed local OCR; exactness not promised | partial (documented review requirement; no OCR run in this audit) |
| Summaries (local excerpt; configured Gemini) | `State/AppStore+AI.swift`, `Core/ProviderClient.swift`, `Core/ProviderContracts.swift` | `core/api.ts`, `core/RevaContext.tsx` (`summarizeRecord`) | `Providers/GeminiService.swift`, `GeminiModels.swift`, `GeminiTransport.swift`, `ProviderRoutes.swift`, `ProviderConfiguration.swift` (WIP model rename) | `server/README.md:82–94`; `docs/architecture.md:86` (WIP); `docs/task-specs/mvp-api-contract.md:10–11`; `r1-07b` (`api.test.ts` 9 tests); server tests not yet run by the runner (`r1-11` build only) | local excerpt works; Gemini requires manual key | partial (provider) / unverified (server tests pending) |
| Visit preparation, citations, stale-report guard | `Core/ReportEngine.swift`, `Features/Preparation/*.swift` (5 files), `State/AppStore+Visits.swift` | `core/domain.ts` (`generateReport`, `reportIsStale`), `features/visits/VisitBrief.tsx`, `VisitDetail.tsx`, `VisitEditor.tsx`, `VisitsPage.tsx`, `visitEdits.ts` | `POST /v1/ai/prepare` (Gemini files above) | `docs/architecture.md:141`; `docs/verification/web-client.md:24`; `apps/web/src/core/COMPATIBILITY.md:10`; `demo/expected-evidence.json`; `r1-07b` (`domain.test.ts` 12, `visitEdits.test.ts` 6, `visitDates.test.ts` 18), `r1-10` (`FixtureEvidenceTests`, `DomainTests`) | deterministic local brief with page citations | working (test level) |
| PDF export / print | `Device/ReportPDFRenderer.swift` | `features/visits/VisitBrief.tsx` print styles | none | `README.md:41`; `docs/verification/README.md:31` (`pdf-share.png`, historical teal); `docs/verification/web-client.md:54` ("a final system print-to-PDF artifact was not captured") | native PDF verified earlier; browser print implemented but not captured | unverified (browser print artifact missing; native evidence historical) |
| Medical profile (quick reference) | `Features/Profile/MedicalProfileEditor.swift`, `MedicalProfileView.swift`, `State/AppStore+Profile.swift` | `features/profile/MedicalProfilePage.tsx` | snapshot passthrough only (`server/README.md:43`) | `docs/architecture.md:141`; `docs/team-workflow.md:106`; `apps/web/README.md:65`; `docs/project-overview-packet.md:124`; `r1-10` (`MedicalProfileTests`) | persistent quick-reference data, not preparation input | working (test level); see C05 |
| Symptom entries | `Core/SymptomEntry.swift`, `Features/Records/SymptomEntryDetailsView.swift`, `SymptomEntryEditorView.swift`, `State/AppStore+Symptoms.swift` | `core/symptoms.ts`, `features/records/SymptomDialog.tsx` | snapshot passthrough | `apps/web/README.md:64`; `docs/verification/profile-symptoms.md`; `docs/team-workflow.md:106`; `r1-10` (`SymptomEntryTests`) | entries are Records and feed preparation | working (test level) |
| Recording, transcription, memory, transcript correction | `Device/AudioServices.swift`, `Features/Visits/RecordingSessionView.swift`, `RecordingDetailView.swift`, `RecordingNotesEditor.swift`, `TranscriptTextEditor.swift`, `State/AppStore+Recordings.swift`, `+Transcription.swift` | `features/visits/RecordingCapture.tsx`, `RecordingDetail.tsx`, `RecordingsPanel.tsx`, `useVisitRecorder.ts` | `Providers/VoiceTranscription.swift`, `VoiceTransport.swift`, `VoiceRoutes.swift` (whisper-1 `verbose_json`) | `docs/task-specs/transcript-correction.md:23–61` (17 harness checks, historical); `apps/web/README.md:68,76`; `server/README.md:96`; `docs/verification/web-client.md:29`; `r1-07b` (`useVisitRecorder.test.ts` 7) | consent-gated capture; transcription needs OpenAI key | partial (provider manual); capture logic working (test level) |
| Booking simulation (local) | `Core/BookingEngine.swift`, `Features/Visits/BookingEditorView.swift`, `BookingStatusView.swift`, `State/AppStore+Bookings.swift` | `features/visits/BookingPanel.tsx` | none | `demo/demo-script.md` step 5 (sampled); `apps/web/README.md:67`; `docs/verification/web-client.md:28`; `r1-07b` (`store.test.ts` call-intent cases) | clearly labeled simulation; no real call | working (test level) |
| Configured live calls (ElevenLabs/Twilio) | `Features/Visits/LiveBookingEditorView.swift`, `LiveBookingStatusView.swift`, `State/AppStore+LiveCalls.swift` | `features/visits/BookingPanel.tsx` (reviewed consent) | `Providers/VoiceCalls.swift`, `VoiceRoutes.swift` (durable receipts `server/README.md:98–100`) | `server/README.md:86–100`; `apps/web/src/core/COMPATIBILITY.md:28`; `.env.example:20–24` | disabled until configured; receipts durable | partial (requires manual provider setup; never exercised) |
| Sync / CAS (push, pull, 409) | `Core/ServerClient.swift`, `State/AppStore+Sync.swift`, `+Providers.swift`, `Features/Shared/SettingsView.swift` | `core/api.ts`, `core/store.ts`, `core/mutations.ts`, `features/SettingsPage.tsx` | `HTTP.swift`, `LocalFileStore.swift`, `PostgresStore.swift`, `Models.swift`, `Configuration.swift` | `server/README.md:31–47`; `docs/architecture.md:68`; `apps/web/README.md:56`; `COMPATIBILITY.md:16–22`; `r1-07b` (`store.test.ts` 18, `api.test.ts` 9), `r1-10` (`TransportTests` 13; `LiveServerTests` skipped) | explicit push/pull, no auto-merge | working (client test level); server suite unverified this audit; live Postgres deferred |
| Browser equivalents (shell, dashboard, settings, responsive) | n/a | `App.tsx`, `features/Dashboard.tsx`, `features/SettingsPage.tsx`, `components/*.tsx`, `styles/*.css` | n/a | `docs/verification/web-client.md:36–42` + `web-responsive-metrics.json` (hash-route era, 25 width combinations); `r1-05`, `r1-06b`, `r1-09` | responsive, palette tokens match | working (build/typecheck); responsive evidence historical (pre-pathname routing) |
| Landing `/`, `/login`, `/signup` (static gate), `/demo` | n/a | `main.tsx:20–35`, `landing/Landing.tsx`, `landing/AccountGate.tsx`, `styles/landing.css`; `scripts/serve.mjs:107` | n/a | `apps/web/README.md:18,52`; `r1-08` (7 wrapper tests incl. routes), `r1-02` (53 browser files have headers, includes landing), `r1-05` | four paths exist locally | WIP (uncommitted); deployment docs contradictory (RVA-01-001) |
| Accounts, sessions, `/app`, per-user Tiger state | none | none (`AccountGate.tsx:9` "Accounts are deferred") | none (no `/v1/auth/*`, no `002_accounts.sql`) | section 2 absence proof; contract lines 71, 88–103, 108–116 | contract only | WIP (concurrent contract) |
| Tiger/PostgreSQL storage (static tokens) | n/a | n/a | `PostgresStore.swift`, `Migrations/001_snapshot.sql`, `Tests/.../PostgresIntegrationTests.swift` (gated) | `server/README.md:49–61,74`; `r1-10`/`r1-11` do not exercise Postgres | implemented, live test gated | partial (never run against a live database) |
| MyChart/FHIR, custom encryption, automatic confirmation, live cellular capture, production certification | — | — | — | `docs/mvp-goal.md:15`; `README.md:103`; `apps/web/README.md:77–78` | excluded | deferred |

## 6. Checklist dispositions

| ID | Status | Files | Evidence | Finding IDs |
| --- | --- | --- | --- | --- |
| A01 | Pass | worktree `git rev-parse HEAD`, `git status --porcelain`, `git worktree list`, `git show --stat cfe0997`; `snapshot-manifest.json`; `.audit-source-delta.patch` | Section 2: HEAD `cfe0997144…`, 13 modified + 4 captured untracked files, manifest 267 ok / 0 mismatch / 0 missing, patch SHA match True (`evidence/00-snapshot-verification.log`), four worktrees listed, WIP delta itemised, account code proven absent | — |
| A02 | Pass with gaps | section 5 matrix (docs + `find` listings + runner logs `r1-02/05/06b/07b/08/09/10/21`) | Every active feature mapped to native/browser/server files and doc evidence; server test suite and Postgres, provider, print and native visual paths remain unverified/partial as marked | RVA-01-006 (evidence gap) |
| A03 | Pass with findings | `docs/reva-style-plan.md:1`, `design/README.md:3,5,18`, `design/palette.json`, `docs/mvp-goal.md:15`, `docs/task-list.md:1,15,16`, `docs/completion-criteria.md:1`, `docs/reva-stack-spec.md:4,14,62,71–75,95,168`, `docs/project-overview-packet.md:3,7,155,168`, `docs/verification/README.md:1,10`, `docs/verification/profile-symptoms.md:11`, `docs/reviews/04-heart-red-palette.md:49`, `docs/deployment-vercel.md:11,18,20`, `docs/build-progress.md:131,157`, `apps/web/README.md:18,34,52`, contract | Section 4 resolves each contradiction: heart-red light palette and active browser supersede teal/dark/web-deferred; accounts/Tiger are emerging scope under the concurrent contract; Identity Platform/Cloud Storage/Tasks/Document AI/SwiftData/50 MB/Vertex are historical | RVA-01-001, RVA-01-002, RVA-01-003, RVA-01-004, RVA-01-007 |
| B03 | Pass with suggestion | `docs/team-workflow.md:13–22,106`; `docs/task-specs/web-client.md:5–10`; `apps/web/README.md:84–91`; contract lines 112–117 | Three-person split is concrete (exclusive feature dirs per person; shared `Core/**`, `src/core/**`, `src/styles/**`, `main.tsx`, `App.tsx`, Dashboard, Settings, `scripts/**`, generators and fixtures are explicitly Captain-owned at line 19; SymptomEntry/Models/palette roles at line 106). Gaps: no owner row for new `apps/web/src/landing/**`; the contract's A/B/C map overlaps Captain- and Person-owned browser files without a reconciliation note | RVA-01-005 |
| C05 | Pass | `docs/architecture.md:141`; `docs/team-workflow.md:106`; `apps/web/README.md:65`; `docs/project-overview-packet.md:124`; `docs/task-specs/mvp-api-contract.md:11`; read-only grep | Documentation is consistent: preparation consumes Records only (symptom entries are Records); profile is quick-reference data and does not rewrite sources. Wire contract `POST /v1/ai/prepare` carries `visit` + `records` only. Read-only `grep -n -i profile` over `Core/ReportEngine.swift`, `State/AppStore+AI.swift`, `Core/ProviderContracts.swift`, `apps/web/src/core/domain.ts`, `core/api.ts` returned no matches. The only profile consumer outside the profile screens is the patient name in configured call requests (`State/AppStore+LiveCalls.swift:25`, `features/visits/BookingPanel.tsx:198,221`, `server/.../VoiceCalls.swift:29,52,153`), matching `project-overview-packet.md:124`. Runtime confirmation belongs to track owners (evidence request ER-01-3) | — |
| L02 | Pass | `docs/deployment-vercel.md:28–32`; `docs/architecture.md:68`; `apps/web/README.md:34,44–52`; `server/README.md:23,29`; root `vercel.json` (no rewrites, CSP `connect-src 'self' blob:`); `apps/web/vercel.json` (three exact rewrites, no `/v1`/`/health`) | Docs state Vercel static hosting does not run `serve.mjs`, the Vite proxy, or Vapor; `/v1` and `/health` need a separately hosted authenticated Swift backend; "Adding API keys to Vercel's frontend build does not create that backend"; the static site "reads no environment variables". Neither `vercel.json` rewrites `/v1` or `/health`, so a deployed page has no backend path today (correct boundary). The contract's future `VITE_REVA_API_ORIGIN` must stay HTTPS-only; noted as WIP guidance | RVA-01-001 (routing contradiction only; the backend boundary itself is correctly documented) |
| N04 | Pass (this track's slice) | this report; `track-01-findings.json`; runner logs | Ranked backlog in section 8; coverage per ID above; every finding carries file/line/trigger/confidence/fix/regression check/owner; historical test counts (`README.md:92`, `docs/verification/web-client.md:11–18`) are consistent with fresh evidence for browser (77 vitest `r1-07b`; 7 wrapper `r1-08`) and native (44 executed, 1 skipped `r1-10`); server counts not yet re-run; no claim here exceeds the cited evidence | all |

## 7. Findings (ordered by severity)

### RVA-01-001 — candidate — P1 — Vercel deployment documentation contradicts itself and the WIP pathname routes; the applicable `vercel.json` has no rewrite for `/demo`, `/login`, `/signup`
- Snapshot: `fable-audit-20260912-114322 @ cfe0997+wip`. Requirement IDs: A03, L02 (routing part), L01 (owner track 16 for the deployed check).
- File/line: `docs/deployment-vercel.md:20` (primary); also `docs/deployment-vercel.md:11,18`, `docs/build-progress.md:157`, `apps/web/README.md:34` versus `apps/web/README.md:52`, `apps/web/src/main.tsx:20–35`, `vercel.json:1–20` (no `rewrites`), `apps/web/vercel.json:4–8`.
- Trigger: follow `docs/deployment-vercel.md` (Root Directory = repository root, "no catch-all rewrite is needed") and deploy the WIP tree; open the deployed `/` landing and click "Log in", "Sign up" or "View the demo" (`Landing.tsx:39–47` link to `/login`, `/signup`, `/demo`), or navigate directly to any of those paths.
- Expected: the single page is served for those paths (as the local wrapper does: `serve.mjs:107`, `r1-08` passed).
- Actual (source-proven contradiction; deployed behaviour predicted, not reproduced): with Root Directory at the repository root only the root `vercel.json` applies, and it contains headers but no `rewrites`; `dist/` (per `r1-21`) has no `login`, `signup` or `demo` file, so a static host returns 404 for those paths. The alternative in `apps/web/README.md:52` (Root Directory = `apps/web`, so the nested `vercel.json` rewrites apply) contradicts `docs/deployment-vercel.md:18`, which requires the repository root because `prepare-assets.mjs` reads `apps/ios/Reva/Resources` outside `apps/web`. The two documented configurations cannot both be followed, and each breaks a different thing.
- Impact: on the deployed browser main entry every landing action would dead-end, and the docs give operators two incompatible instructions. Local behaviour is unaffected.
- Evidence: `sed -n '11p;18p;20p' docs/deployment-vercel.md`; `sed -n '157p' docs/build-progress.md`; `sed -n '34p;52p' apps/web/README.md`; `cat vercel.json` (no `rewrites` key); `cat apps/web/vercel.json`; `sed -n '20,35p' apps/web/src/main.tsx`; `git diff -U0 -- apps/web/README.md` (line 52 added by the WIP delta; line 34 committed in cfe0997); `evidence/r1-21-dist-inventory.log` (dist root contains only `index.html` and `reva.svg`).
- Confidence: medium (documentation contradiction is certain; the 404 outcome relies on Vercel's static-output behaviour, which this audit may not exercise).
- Smallest fix: keep the repository-root model; add exact rewrites for `/demo`, `/login`, `/signup` (and later `/app`) to the root `vercel.json` while leaving `/v1`, `/health`, `/assets`, `/demo/*` files and `/ocr/*` unrewritten; delete or empty `apps/web/vercel.json`; correct `docs/deployment-vercel.md:20`, `docs/build-progress.md:157` and `apps/web/README.md:52` to describe the pathname routes and the single config location.
- Regression check: `node --test apps/web/scripts/serve.test.mjs` (local wrapper parity) and, owner-run, a Vercel preview deployment `curl -sI https://<preview>/login` expecting 200 with `text/html`, plus `curl -sI https://<preview>/v1/state` expecting no HTML rewrite.
- Owner track: Captain (shared config, root `vercel.json`, `main.tsx`, delivery docs).

### RVA-01-002 — suggestion — P3 — Historical planning documents lack superseded banners and still assert deferred browser, Identity Platform without password storage, Cloud Storage/Tasks/Document AI, SwiftData and a 50 MB limit
- Snapshot as above. Requirement IDs: A03.
- File/line: `docs/reva-stack-spec.md:4` (primary; no banner, "authorizes no implementation"); `docs/reva-stack-spec.md:14,62,71–75,95,168`; `docs/mvp-goal.md:15` ("web wrapping" deferred); `docs/project-overview-packet.md:7,155` ("No production web app", "No … browser wrapper"); `docs/verification/profile-symptoms.md:11` (Sky/Ivory "now").
- Trigger: a new contributor (or the concurrent accounts implementer) reads these files for current scope.
- Expected: each historical file states at its head which later document supersedes it, as `docs/completion-criteria.md:1`, `docs/task-list.md:1` and `docs/verification/README.md:1` already do.
- Actual: `reva-stack-spec.md`, `mvp-goal.md`, `project-overview-packet.md` (beyond its `d0af1df` baseline note at line 3) and `profile-symptoms.md` carry no such banner; the stack spec's identity row (line 71) directly contradicts the concurrent contract's bcrypt/session design.
- Impact: scope confusion only; no runtime effect.
- Evidence: `sed -n '1,4p;14p;71,75p' docs/reva-stack-spec.md`; `sed -n '15p' docs/mvp-goal.md`; `sed -n '3p;7p;155p' docs/project-overview-packet.md`; `sed -n '11p' docs/verification/profile-symptoms.md`; `sed -n '1p' docs/completion-criteria.md docs/task-list.md docs/verification/README.md`; `sed -n '49p' docs/reviews/04-heart-red-palette.md`.
- Confidence: high. Smallest fix: one blockquote line at the top of each file pointing to `docs/reva-style-plan.md:1`, `docs/architecture.md`, `docs/task-specs/web-client.md` and the accounts contract. Regression check: `grep -n -m1 '^> ' docs/reva-stack-spec.md docs/mvp-goal.md docs/project-overview-packet.md docs/verification/profile-symptoms.md`. Owner track: Captain (delivery docs).

### RVA-01-003 — WIP — P3 — Packet PDF still says `gemini-2.5-flash` after the WIP delta renamed the model in its Markdown source and generator
- Snapshot as above. Requirement IDs: A03, N04.
- File/line: `docs/project-overview-packet.md:168` (WIP-edited to `gemini-3.8-flash`); `README.md:76` ("Rebuild both with `scripts/build_overview_packet.py`"); `output/pdf/reva-project-overview-packet.pdf` page 6 (`gemini-2.5-flash.`), unchanged in the delta.
- Trigger: the WIP model rename is committed as-is. Expected: PDF and Markdown packet agree. Actual: `pypdf` text extraction (read-only) of the checked-in PDF finds `gemini-2.5-flash.` on page 6 and `baseline d0af1df` on every page; the Markdown source now says `gemini-3.8-flash` while still declaring `Code baseline d0af1df` (line 3), which no longer describes its content.
- Impact: stale deliverable; no runtime effect.
- Evidence: `git diff -U0 -- docs/project-overview-packet.md` (line 168 changed); `git status --porcelain` (PDF not modified); read-only extraction: `python3 -c "import pypdf,re;r=pypdf.PdfReader('output/pdf/reva-project-overview-packet.pdf');[print(i,m) for i,p in enumerate(r.pages,1) for m in re.findall(r'gemini[-\w.]*',p.extract_text() or '',re.I)]"` → `(6, 'gemini-2.5-flash.')`.
- Confidence: high. Smallest fix: before committing the rename, regenerate the PDF with `scripts/build_overview_packet.py` (owner-run; the audit runner must not) or revert the packet edit and add a dated note. Regression check: repeat the extraction and expect `gemini-3.8-flash`. Owner track: Captain (scripts/config, docs).

### RVA-01-004 — suggestion — P3 — Root README and architecture doc do not describe the new `/`, `/demo`, `/login`, `/signup` structure
- Snapshot as above. Requirement IDs: A03, A02.
- File/line: `README.md:34` (primary: "Open http://127.0.0.1:5173" only); `README.md:103`; `docs/architecture.md` (no route or landing section; the contract line 116 plans one).
- Trigger: a reader opens the root README to run the browser. Expected: the four paths are named as in `apps/web/README.md:18`. Actual: only the browser guide (WIP line 18) documents them; the root README and `docs/architecture.md` describe the pre-landing single entry.
- Impact: documentation drift only. Evidence: `sed -n '24,34p;103p' README.md`; `grep -n -E 'landing|/login|/signup|/demo' docs/architecture.md README.md` (no matches in either). Confidence: high. Smallest fix: one sentence in `README.md:34` and one row in the `docs/architecture.md` client section. Regression check: the grep returns matches. Owner track: Captain.

### RVA-01-005 — suggestion — P3 — Ownership table has no owner for `apps/web/src/landing/**`, and the concurrent contract's A/B/C split overlaps Captain-owned browser files without reconciliation
- Snapshot as above. Requirement IDs: B03.
- File/line: `docs/team-workflow.md:19` (Captain row lists `src/core/**`, `src/components/**`, `src/styles/**`, `App.tsx`, `main.tsx`, Dashboard and Settings; no `landing/**`); contract lines 112–116 (B owns all `apps/web/**`; C edits `docs/architecture.md`, root `README.md`, root `.env.example`, which line 19 assigns to the Captain).
- Trigger: the accounts implementer (contract role B) edits `SettingsPage.tsx`, `main.tsx` or `core/store.ts` (contract lines 92–101) while a team member follows `team-workflow.md`. Expected: one owner per file. Actual: two ownership maps apply to the same files; `landing/**` and `styles/landing.css` are unassigned in the team guide (`styles/**` is Captain's by pattern; `landing/**` is not covered by any pattern).
- Impact: coordination risk only (the contract forbids commits, so the user's review is the merge gate).
- Evidence: `sed -n '13,22p;106p' docs/team-workflow.md`; contract `sed -n '112,117p'`; `find apps/web/src/landing -type f`. Confidence: high. Smallest fix: add a line to `docs/team-workflow.md` mapping contract roles A/B/C onto Person 3 / Captain(+Person 1–2 by feature dir) / Captain and declaring `apps/web/src/landing/**` and `styles/landing.css` Captain-owned. Regression check: none (doc). Owner track: Captain.

### RVA-01-006 — evidence-gap — P3 — No native heart-red visual capture exists; all native screenshots are teal-era
- Snapshot as above. Requirement IDs: A02, A03 (K-series palette checks are owned by another track).
- File/line: `docs/reva-style-plan.md:1` (primary: "the heart-red palette awaits its own visual check in the running MVP"); `docs/verification/README.md:28–34` and `docs/verification/screenshots/` (native captures `summary-exact-palette.png`, `medical-profile.png`, `visit-navigation.png`, `sample-transcript.png`, `implant-page-2.png`, `pdf-share.png`, `provider-fixture-brief.png`, `summary-dark.png` are all pre-`678e742`); `docs/architecture.md:143` ("Both clients now use exact blush ivory"); `docs/build-progress.md:139`; `docs/verification/web-client.md:17` (simulator build passed, no visual check recorded).
- Trigger: asserting that the native app renders the heart-red palette. Expected: at least one native capture or measured token check after `678e742`. Actual: only browser tokens are verified (`web-client.md:42`, `web-desktop*.png`, `web-phone*.png`); the docs themselves say the native check is pending.
- Impact: unverified claim, not a defect. Evidence: `sed -n '1p' docs/reva-style-plan.md`; `sed -n '28,34p' docs/verification/README.md`; `ls docs/verification/screenshots`; `grep -n -i 'heart' docs/build-progress.md`. Confidence: high that the evidence is absent. Smallest fix: owner captures one simulator screenshot per tab after `678e742` and records it in `docs/verification/`. Regression check: the capture exists and its sampled canvas pixel equals `#FBF7F5`. Owner track: Person 1 (records/profile screens) with Captain (Theme.swift); simulator work is owner-run, not the audit runner.

### RVA-01-007 — suggestion — P3 — Four relative links in `docs/reviews/acceptance-audit.md` point at moved native files
- Snapshot as above. Requirement IDs: A03, N04.
- File/line: `docs/reviews/acceptance-audit.md:33` (`../../apps/ios/Reva/UI/RecordingViews.swift#L74`, `#L92`), `:35` (`../../apps/ios/Reva/UI/RecordsView.swift#L46`), `:41` (`../../apps/ios/Reva/UI/RecordsView.swift#L16`).
- Trigger: click the links. Expected: resolve to the current `apps/ios/Reva/Features/Visits/…` and `Features/Records/RecordsView.swift`. Actual: `apps/ios/Reva/UI/` does not exist in the snapshot (`find apps/ios -name '*.swift'` lists `Features/**` only). A scratchpad link checker over `README.md`, `design/README.md` and all `docs/**/*.md` checked 144 relative links; these 4 are the only broken ones. The file's header (line 3) dates it to checkpoint `7d0f585` but does not say its paths are historical.
- Impact: navigation only. Confidence: high. Smallest fix: add "paths as of `7d0f585`" to line 3 or update the four targets. Regression check: rerun the link check (script kept in the scratchpad; not in the repo). Owner track: Captain (delivery docs).

Refuted candidates (not recorded as findings): (a) `README.md:92` historical test counts — consistent with fresh runner evidence (77 vitest in `r1-07b`, 7 wrapper tests in `r1-08`, 44 native executed with 1 skipped in `r1-10`); server "30 tests" not yet re-run, so no contradiction is provable. (b) `design/README.md:18` "The shipped MVP uses Sky canvas…" sits under the heading "Supplied swatch palette (superseded, retained)" at line 5 and is therefore adequately marked. (c) `docs/deployment-vercel.md:20` "Missing `/v1` or OCR files must not be rewritten" — both `vercel.json` files honour this (no catch-all). (d) `docs/architecture.md:68` "HTTPS termination is a hosting setup step" agrees with `server/README.md:23`; no contradiction. (e) The initial `r1-07-vitest` failure (missing `public/demo/*.json`) is an ordering artefact resolved by `r1-07b` after `npm run build`; the documented order in `apps/web/README.md:11–15` runs `npm run assets` first, so the docs are correct.

## 8. Ranked repair backlog (N04, this track's slice) and evidence requests

Ranked backlog: 1. RVA-01-001 (P1 candidate, Captain) — reconcile Vercel root/rewrites before the WIP is committed or deployed. 2. RVA-01-003 (P3 WIP, Captain) — regenerate or revert the packet PDF with the model rename. 3. RVA-01-005 (P3, Captain) — publish one ownership map covering contract roles and `landing/**`. 4. RVA-01-002 / RVA-01-004 / RVA-01-007 (P3, Captain) — banners, README route note, four links. 5. RVA-01-006 (evidence gap, Person 1 + Captain) — native heart-red capture.

Evidence requests for the runner (track 17) or owners; none were executed by this track except the read-only reads noted:

| ID | Command (cwd = frozen worktree unless stated) | Purpose | Priority | Related |
| --- | --- | --- | --- | --- |
| ER-01-1 | `cd server && swift test -j 4` (loopback only; `REVA_TEST_DATABASE_URL` unset so the live Postgres test skips) | Re-establish the server test count and confirm `GeminiProviderTests` asserts the WIP default `gemini-3.8-flash` | high | matrix rows Summaries/Sync; N04 |
| ER-01-2 | Owner-run only (the runner must not contact Vercel): after a preview deployment of the WIP tree with Root Directory = repository root, `curl -sI https://<preview>/login`, `/signup`, `/demo`, and `curl -sI https://<preview>/v1/state` | Confirm or refute the predicted 404s and that `/v1` is not rewritten | high | RVA-01-001 |
| ER-01-3 | `grep -n -i 'profile' apps/ios/Reva/Core/ReportEngine.swift apps/ios/Reva/State/AppStore+AI.swift apps/ios/Reva/Core/ProviderContracts.swift apps/web/src/core/domain.ts apps/web/src/core/api.ts server/Sources/RevaServer/Providers/GeminiModels.swift` | Log into `commands.jsonl` the read-only proof (already observed by this track: no matches) that preparation does not consume profile facts | medium | C05 |
| ER-01-4 | `python3 -c "import pypdf,re;r=pypdf.PdfReader('output/pdf/reva-project-overview-packet.pdf');print([(i,m) for i,p in enumerate(r.pages,1) for m in re.findall(r'gemini[-\w.]*',p.extract_text() or '',re.I)])"` | Log the stale-PDF proof (observed: page 6 `gemini-2.5-flash.`) | low | RVA-01-003 |
| ER-01-5 | `diff <(python3 -c "import json;print(sorted(json.load(open('vercel.json')).keys()))") <(python3 -c "import json;print(sorted(json.load(open('apps/web/vercel.json')).keys()))")` and `cat vercel.json apps/web/vercel.json` | Record that the root config has no `rewrites` and the nested one has only three exact rewrites | medium | RVA-01-001, L02 |
| ER-01-6 | Owner-run only (no simulator for the runner): one simulator screenshot per tab after `678e742`, saved under `docs/verification/` | Close the native heart-red evidence gap | low | RVA-01-006 |
| ER-01-7 | `python3 /private/tmp/claude-502/-Users-tempadmin-Documents-Reva/a4829c72-eed5-4457-959e-34f8c5715bdc/scratchpad/linkcheck.py` (read-only; opens files only) | Reproduce the 4-of-144 broken-link count | low | RVA-01-007 |

## 9. What was NOT reviewed and why

- Binary files in scope: `docs/verification/orthopedic-brief.pdf` and the 12 PNG screenshots were listed but not opened (image content is not needed for traceability; palette measurement belongs to the K-series owner).
- `output/pdf/reva-project-overview-packet.pdf` was only text-extracted and grepped for model/baseline strings, not read.
- `demo/README.md` (lines 1–40) and `demo/demo-script.md` (lines 1–30) were sampled only for feature names; the rest of `demo/**` (generators, fixtures, QA JSON) belongs to other tracks.
- Application source under `apps/ios`, `apps/web/src` (other than `main.tsx`, `landing/*.tsx`, `styles/landing.css` header, `core/COMPATIBILITY.md`), `server/Sources`, `scripts`, `Tests` was inventoried by filename only; the two read-only greps in section 6 (C05) and section 2 (account markers) are the only content reads. Correctness of those files is outside this track.
- Live main checkout: nothing read except `docs/task-specs/accounts-and-tiger.md` (permitted contract file).
- No servers, simulators, providers, databases or Vercel were contacted; no build or test was run by this track (runner logs cited instead). Native and browser UI behaviour was not observed.
- The `evidence/r1-11-swift-build-server.log` was read only at its tail (build in progress at read time); server test results are pending (ER-01-1).

## 10. Statement on actionable defects

One candidate defect survived refutation: RVA-01-001 (P1, candidate, confidence medium) — a source-proven contradiction between `docs/deployment-vercel.md:11,18,20` / `docs/build-progress.md:157` / `apps/web/README.md:34` and the WIP `apps/web/README.md:52` / `apps/web/vercel.json` / `main.tsx` pathname routes, whose deployed consequence (404 on `/login`, `/signup`, `/demo` under the documented root configuration) is predicted from the committed `vercel.json` and `dist` inventory but was not reproduced. No P0 finding. The remaining items are one WIP artefact (RVA-01-003), one evidence gap (RVA-01-006) and four P3 suggestions (RVA-01-002, -004, -005, -007). Assigned checklist IDs A01, C05 and L02 pass at documentation level with the evidence above; A02, A03, B03 and N04 pass with the recorded gaps and suggestions.
